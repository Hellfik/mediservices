{% extends 'adminBase.html.twig' %} {% block title %}Nouveau Client{% endblock %} {% block body %}
<div class="container-fluid bandeau bg-secondary">
    <div class="container">
        <h3 class="text-dark"><i class="fa fa-users" aria-hidden="true"></i> Ajouter un client</h3>
    </div>
</div>
<div class="card col-md-8 mx-auto my-5 px-0">
    <div class="card-header bg-secondary text-dark">
        <h2>Ajouter un client </h2>
    </div>
    <div class="card-body">
        <div class="container">
            <div class="card-text">
                {{ include('client/_form.html.twig' , {'button_label': 'Ajouter'}) }}
            </div>
        </div>
    </div>
    <a class="btn btn-primary btn-sm" href="{{ path('client_index') }}">Retour à la liste</a>
</div>
{% endblock %}